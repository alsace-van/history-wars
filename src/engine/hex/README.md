# engine/hex/

Systeme de coordonnees hexagonales cubique. Toutes les fonctions prennent `hexSize` en parametre, jamais hardcode.

A implementer Phase 0 sous-tache 0.6 :
- `coordinates.ts` axial / cubique / world conversions
- `distance.ts` distance entre 2 hex
- `neighbors.ts` voisins direct, dans rayon
